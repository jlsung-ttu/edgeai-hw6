# Container runtime deps for Dockerfile.ci's `pip install -r requirements.txt`.
# This file is hand-maintained — NOT pdm-exported. Heavy ML packages live
# here (not in pyproject.toml's [project].dependencies) so the host PDM
# venv stays small and the Docker image stays the only place these get
# installed. Pinned versions match the dustynv/pytorch:2.7-r36.4.0 base
# image at the time the lab was authored.
ultralytics==8.3.40
opencv-python==4.10.0.84
paho-mqtt==2.1.0
onnxruntime==1.20.0
numpy==1.26.4

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.coverage
htmlcov/

# Build artifacts
*.engine
*.onnx
# ...except the INT8 engine deliverable from Part 0 (committed)
!best_int8.engine

# Calibration sample frames (Part 0) — large, gitignored, regenerated
# from Lab 9 dataset on the Jetson per calibration/README.md
calibration/calibration_data/

# TensorRT calibration cache + Ultralytics image-scan caches —
# regenerated whenever calibration runs against a new best.pt.
*.cache

# Ultralytics validation/inference output — `yolo val ...` writes
# runs/detect/val-N/ on each invocation. Regenerable.
runs/

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Local secrets
.env

# PDM
.pdm-python
.pdm-build/
__pypackages__/
